var through = require('through')

module.exports = function (C) {
  var delay = 1500

  // align element with targeted interface / input

  var s = through(function Write (d) {

    // examine d.value and move from there! 
    // draw beside target element!
    
    C.draw(d)

    setTimeout(function () {
      C.erase()
    }, delay)
  })

  return s
}
